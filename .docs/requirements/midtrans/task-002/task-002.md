1. we can create a test purchase to trigger snap anytime. Based on current setting, if all has been setup properly, it is possible to setup a test purchase button. For production we can set warning that its using real money. For staging set warning that its a test purchase. Make a dummy purchase with three objects named test_something. Then it opens in a new tab the payment url. 
2. More than one environemnt per project is a bit too overscoped. Since we only are provided with Production and Sandbox from midtrans. But it can always be utilised as such. Just ensure each environment has boolean IsSandbox so that if its true, it creates a sandbox call. If false, creates production. On creation of project/application, it would create production (IsSandbox = false) and staging (IsSandbox = true). Other new environemnt should be automatically a sandbox, regardless of name. 
3. Each payment call is registered in database and the status. I think now we are overriding the endpoint of the webhook. I think what i want is to always webhook to this server, server updates the status, then forward the data to the registered/saved webhook url per application environment. 